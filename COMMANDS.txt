
# generate count=<number> name=<string>
- Generate x amount of empty rows with one column $1, unless name is provided.

# eval <identifier>=<value>
- Create a new column which will fill all its cells with a value.